/*
    The machine, IDK if i'll use this yet.
*/

use crate::register::Register;
use std::collections::HashMap;

/*
pub struct Machine {
    pub registers: HashMap<&str, Register>,
}
*/

/// Given a line, execute it.
fn exec() -> () {
    todo!();
}
